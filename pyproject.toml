[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "video-demo"
version = "0.1.0"
description = "Local video event and person tracking MVP"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
video-demo = "video_demo.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
